# LLM serving — points at an OpenAI-compatible endpoint
# (e.g. a vLLM server running on a RunPod Pod). Copy this file
# to backend/.env and fill in your values.

LLM_BASE_URL=https://your-pod-id-8000.proxy.runpod.net/v1
LLM_API_KEY=your_vllm_api_key_here
LLM_MODEL=meta-llama/Llama-3.1-8B-Instruct
